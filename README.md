# Rest API-Like Local API Example, also have MySQL connection

## Configuration

Inside the "node-js-api" folder, open new terminal or command line and type:

```shell
$ npm install
```
After that, go to "src folder" and type:

```shell
$ cd src
$ node App.js
```
